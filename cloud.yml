kind: project
name: WarungTosku

environments:
  production:
    database:
      type: postgresql
      version: 16
    build:
      - composer install --no-dev --optimize-autoloader
    deploy:
      - php artisan config:clear
      - php artisan migrate --force
